[tox]
env_list = py311, py312, py313, pkg
skip_missing_interpreters = true
isolated_build = true

[testenv]
description = Run the pytest suite with {basepython}
extras = test
commands = pytest -q

[testenv:pkg]
description = Build release artifacts and verify PyPI metadata
extras = release
commands =
    python -m build --sdist --wheel --outdir {envtmpdir}/dist
    python -m twine check {envtmpdir}/dist/*
